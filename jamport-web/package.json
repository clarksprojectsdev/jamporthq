{
  "name": "jamport-web",
  "version": "0.1.0",
  "private": true,
  "description": "Static HTML landing for JamPort Media Services — deploy separately from the Expo app",
  "scripts": {
    "dev": "echo 'Open index.html in a browser — no build step'",
    "build": "echo 'Static site — no npm build'",
    "start": "echo 'Open index.html in a browser'"
  }
}
